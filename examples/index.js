import React from "react";
import ReactDOM from "react-dom";
import Editor from "../src";

const rootElement = document.getElementById("root");

ReactDOM.render(React.createElement(Editor), rootElement);
